import React from 'react';
import { connect } from 'react-redux';

export default (WrappedComponent) => {
    class ProductPage extends React.Component {

        constructor(props) {
            super(props);
            
        }








    }
    const mapStateToProps = () => {

    }
    const mapDispatchToProps = {

    }
    return connect()(ProductPage);
}
